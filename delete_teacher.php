<?php
// Підключення до бази даних
require_once 'conect.php';

// Перевірка, чи були передані дані з форми
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    // Отримання даних з форми (припустимо, що вибір викладача здійснюється через випадаючий список)
    // Вам слід замінити цей код на власний відповідно до вашої реалізації

    // Приклад:
    $teacher_id = $_POST['teacher_id']; // Припустимо, що вибір викладача здійснюється через випадаючий список

    // Видалення викладача з бази даних
    $sql = "DELETE FROM Teachers WHERE id=$teacher_id";

    if (mysqli_query($conn, $sql)) {
        echo "Викладач успішно видалений";
    } else {
        echo "Помилка: " . $sql . "<br>" . mysqli_error($conn);
    }

    // Закриття підключення до бази даних
    mysqli_close($conn);
}
?>
